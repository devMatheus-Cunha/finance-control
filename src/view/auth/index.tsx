import React from "react";

// container
import { Auth } from "../../container/auth";

// -------------------------------------------------
// Export Function
// -------------------------------------------------
const auth = () => {
	return (
		<Auth />
	);
}

export default auth;
