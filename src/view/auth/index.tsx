import React from "react";

// container
import { Auth } from "../../container/auth";

// -------------------------------------------------
// Export Function
// -------------------------------------------------
const auth = () => <Auth />;

export default auth;
